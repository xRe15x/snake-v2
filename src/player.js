function player() {
    const segments = [] // Array of objects {x: int, y: int}, [0] will always be the head
};

function playerUI() {
    function drawSnake() {}; // Shorthand to call drawHead() and drawBody();

    function drawHead() {};
    function drawBody() {};
}

export default player;